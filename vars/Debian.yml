---
buildah_pkg_mgr: apt
buildah_apt_name: devel:kubic:libcontainers:stable
buildah_apt_mirror: download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable/{{ ansible_distribution }}_10
buildah_apt_url: deb http://{{ buildah_apt_mirror }}/ /
buildah_apt_key_url: https://{{ buildah_apt_mirror }}/Release.key
